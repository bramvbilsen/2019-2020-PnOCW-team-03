{
  "name": "multiscreen_casting",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prestart": "copyfiles -u 1 ./static/**/*.html ./build/static",
    "start": "tsc && node build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.1",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "copyfiles": "^2.1.1"
  }
}
