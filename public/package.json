{
    "name": "public",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "build": "tsc && copyfiles ./**/*.html ./build && copyfiles ./**/*.css ./build && copyfiles ./**/*.png ./build  && copyfiles ./**/*.jpg ./build && copyfiles ./**/*.jpeg ./build && copyfiles ./**/*.svg ./build",
        "test": "mocha -r ts-node/register -r jsdom-global/register tests/**/*.spec.ts",
        "gulp": "gulp",
        "start": "gulp"
    },
    "devDependencies": {
        "copyfiles": "^2.1.1",
        "gulp": "^4.0.2",
        "gulp-exec": "^4.0.0",
        "gulp-typescript": "^5.0.1",
        "ts-node": "^8.5.0",
        "typescript": "^3.6.3"
    },
    "author": "",
    "dependencies": {
        "@types/jquery": "^3.3.31",
        "@types/socket.io-client": "^1.4.32",
        "axios": "^0.19.0",
        "browserify": "^16.5.0",
        "line-intersect": "^2.2.3",
        "socket.io-client": "^2.3.0",
        "tsify": "^4.0.1"
    }
}
