/**
 * Updates each slaveScreen's htmlCanvas with the corresponding image cut from the original image.
 * Returns the
 * @param {Array} list - a deepcopy list with all slaves and their updated coordinates in relation to the img to cast.
 * AKA: sizeConverter.js creates this list.
 * @param {number} height - The height of the image to cast.
 * @param {number} screenWidth - The width of the image with the screens.
 * @param {number} screenHeight - The height of the image with the screens.
 * @param {array} list - A list with the points of the screens.
 * @param {boolean} center - Whether or not to center the image.
 */

export function cutImagePerScreen(Array:list, Blob: image){
    let imagePieces = []

    return
}